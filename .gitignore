# Dependencies
/node_modules

# Build artifacts
/dist
/build

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# User data / Database
# ไม่ควรเก็บไฟล์ฐานข้อมูลขึ้น GitHub ถ้ามีข้อมูลสำคัญ
# แต่ถ้าเป็นฐานข้อมูลเปล่าๆ สำหรับโครงสร้าง ก็เอาบรรทัดล่างออกได้
database.sqlite
*.sqlite
db.json.migrated

# Uploads directory from server.js
/uploads

# IDE config files
.vscode
.idea

# Environment variables - ห้ามเอาขึ้น GitHub!
.env
.env.local
.env.production

# OS generated files
.DS_Store
Thumbs.db